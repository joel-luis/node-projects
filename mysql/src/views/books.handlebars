<h1>Livros disponíveis</h1>
<div class="books-container">
 {{#each books}}
  <div>
    #{{this.id}} - 
    <span><a class="link" href="books/{{this.id}}">{{this.title}}</a></span>
    - {{this.pageqty}} páginas
  </div>
 {{/each}}
   <a href="/" class="link-back">Adicionar livro</a>
</div>